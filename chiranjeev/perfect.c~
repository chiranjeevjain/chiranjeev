#include<stdio.h>
void main()
{
	int num,sum,i;
	sum  = 0;
	printf("\n enter the number to be checked\n");
	scanf("%d",&num);
	for(i=1;i<=num/2;i++)
	{
		if(num%i == 0)
			sum += i;
	}
	if(sum == num)
		printf("\n it is a perfect number\n");
	else
		printf("\n it is not a perfect number\n");
	
}
